# <set> <preset> — sets listed here get the preset's ffmpeg chain applied when
# their WAVs are converted to public/sounds/*.webm (normalize_audio.sh). The
# source WAVs stay clean; preview with build-tools/preview-audio-preset.sh.
# Presets are defined in audio-presets.sh.
nasa apollo
